---
title: Local lifecycle
description: What boson start manages and how local state behaves.
order: 5
---

`boson-orchestration` is the private control plane behind the small CLI
surface. It keeps infrastructure, migrations, process supervision, health
gates, logs, and shutdown behavior consistent across every Boson project.

## Startup sequence

```text
validate project and tools
          ↓
start PostgreSQL + Dashboard
          ↓
wait for infrastructure health
          ↓
build migrate + Server + Worker
          ↓
apply all pending migrations
          ↓
start and health-gate processes
          ↓
stream logs and supervise
```

Infrastructure and Server readiness each have a 60-second health window.
Startup rolls back managed infrastructure if building, migration, process
launch, or a health gate fails.

## Supervision

Boson holds a per-project lifecycle lock while the stack runs. A second
`boson start` exits with guidance to use `boson status` or `boson logs`.

The foreground supervisor checks Server and Worker every second. If either
exits unexpectedly, Boson reports the failed service and points to its logs.

## Graceful shutdown

Ctrl+C and `boson stop` follow the same shutdown path:

1. Signal Server and Worker.
2. Allow up to ten seconds for each process to exit.
3. Stop managed Dashboard and PostgreSQL containers.
4. Stop log forwarding tasks.
5. Clear transient lifecycle state.

PostgreSQL volumes, uploaded files, and local mail remain on disk.

Graceful process signaling is currently supported on macOS and Linux. Other
platforms may report that graceful stop is not yet available.

## State and logs

Transient lifecycle state is stored under `.boson/run`:

- `.boson/run/state.json` — supervised units, ports, and versions
- `.boson/run/lifecycle.lock` — exclusive lock for one `boson start` per project

Unified service logs are appended under `.boson/logs/<unit>.log`. Both
directories are gitignored.

```bash
boson status
boson logs
boson logs server --follow
```

If a previous supervisor crashed, the next lifecycle command detects stale
state. `boson stop` also cleans up orphaned managed infrastructure when the
recorded supervisor is no longer alive.

## Troubleshooting

| Symptom | What to do |
| --- | --- |
| `Boson is already running for this project` | Use `boson status` or `boson logs`; stop with `boson stop` |
| Docker daemon is not running | Open Docker Desktop and wait until Docker reports ready |
| Server or Worker exited unexpectedly | Inspect `boson logs server` or `boson logs worker` |
| Service did not become healthy | Check that service's logs; readiness waits up to 60 seconds |
| Port already in use | `boson doctor` reports this as a warning; close the owner or stop an existing Boson stack |
| Required doctor check failed | Apply the printed `fix:` line, then retry |

## Infrastructure boundary

Docker Compose is the current infrastructure backend. The orchestration crate
hides it behind an `InfrastructureBackend` contract, leaving room for future
Docker Engine, Podman, or externally managed implementations without changing
the CLI.
